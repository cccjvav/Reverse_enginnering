{
  "scope": "The assembled skeleton typechecked through the author's own shipped tsconfig.json, with no custom module resolver. Diagnostic only.",
  "workspace": ".work/skeleton",
  "configUsed": "extensions/shuncode/tsconfig.json (shipped verbatim)",
  "hostTypesOverlay": true,
  "hostTypesCaveat": "The Chat surface comes from community/host-types, which is RECONSTRUCTED from the author's code, not their original declarations. A zero here means the reconstruction satisfies their sources, not that the original host was recovered.",
  "droppedFromFiles": [
    "../../src/file-tool-registry.ts",
    "../../src/ide-tool-definitions.ts",
    "test/workspace-hub-store.test.ts"
  ],
  "droppedReason": "Declared by the author but never recovered: two B-layer modules exist only as compiled .js (reached via their .d.ts instead), and the test file did not ship.",
  "typescriptVersion": "5.9.3",
  "filesCompiled": 31,
  "coverage": {
    "extensionSources": {
      "total": 34,
      "reached": 33,
      "notReached": [
        "codex-account-view.ts"
      ]
    },
    "coreModules": {
      "total": 41,
      "reached": 26,
      "notReached": [
        "apply-patch.js",
        "build-info.js",
        "canonical-diff.js",
        "custom-tool-manifest.js",
        "find-files.js",
        "managed-command-risk.js",
        "mcp-protocol.js",
        "read-files.js",
        "read-image.js",
        "ripgrep-diagnostics.js",
        "search-files.js",
        "snapshot-build-metadata.js",
        "snapshot-packaged-ripgrep.js",
        "snapshot-sdk-versions.js",
        "workspace-paths.js"
      ]
    },
    "note": "Modules not reached are simply not imported from the extension entry graph: the file tools (apply-patch, read-files, find-files, search-files, read-image) are called by the bridge at runtime rather than imported by the extension, and codex-account-view.ts is not imported by anything. They are typechecked separately by npm run verify:btypes, so they are not unverified - just outside this program."
  },
  "errorCount": 0,
  "counts": {},
  "errorsBySource": {},
  "unresolvedModuleCount": 0,
  "layoutResolves": true,
  "layoutVerdict": "Every cross-layer import resolved with no custom resolver, so the directory layout read out of the author's tsconfig is correct.",
  "doesNotClaim": "Typechecking is not building. This produces no bundle, runs no npm install, and does not touch the Code OSS carrier.",
  "errors": []
}
